doctype html
html
  head
  body#body
    header
      = render 'header'
    main
      = content_for?(:admin_layout) ? yield(:admin_layout) : yield
    footer
      = render 'footer'
    = javascript_include_tag 'application'
